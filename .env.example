VITE_API_BASE_URL=/api/v1
VITE_USE_MOCKS=true
VITE_API_TIMEOUT_MS=15000

# Spring Boot origin used by the Vite dev proxy. Once the backend is hosted,
# set VITE_USE_MOCKS=false and point this at the real URL.
VITE_DEV_API_PROXY=http://localhost:8080
